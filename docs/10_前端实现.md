# 前端实现

**版本：** v1.0
**更新日期：** 2026-05-22

---

## 1. 目录结构

```
frontend/
├── public/
│   └── index.html
├── src/
│   ├── main.tsx                     # 入口：ReactDOM.createRoot + Router
│   ├── App.tsx                      # 路由配置（React Router v6）
│   │
│   ├── pages/                       # 页面级组件（与路由一一对应）
│   │   ├── HomePage.tsx             # /
│   │   ├── SearchPage.tsx           # /search
│   │   ├── RestaurantDetailPage.tsx # /restaurant/:id
│   │   ├── DietLogPage.tsx          # /diet/log
│   │   ├── DietProfilePage.tsx      # /diet/profile
│   │   ├── SavedPage.tsx            # /saved
│   │   ├── LoginPage.tsx            # /auth/login
│   │   └── RegisterPage.tsx         # /auth/register
│   │
│   ├── components/                  # 复用组件
│   │   ├── layout/
│   │   │   ├── AppHeader.tsx        # 顶部导航（搜索框 + 用户菜单）
│   │   │   └── AppLayout.tsx        # 整体布局包裹
│   │   ├── search/
│   │   │   ├── SearchBar.tsx        # 搜索框 + Autocomplete
│   │   │   ├── FacetPanel.tsx       # 左侧 Facet 筛选面板
│   │   │   ├── ResultCard.tsx       # 单个搜索结果卡片
│   │   │   ├── ResultList.tsx       # 结果列表 + 分页
│   │   │   ├── SortSelector.tsx     # 排序模式切换
│   │   │   └── RelevanceExplainer.tsx  # "为什么推荐"展开面板
│   │   ├── diet/
│   │   │   ├── FoodSearchModal.tsx  # 食物搜索弹窗（含 LLM 确认）
│   │   │   ├── LlmConfirmCard.tsx   # LLM 估算数据确认卡
│   │   │   ├── LogEntry.tsx         # 单条饮食日志条目
│   │   │   ├── NutritionBar.tsx     # 营养进度条（热量/蛋白质/脂肪/碳水）
│   │   │   └── MealGroup.tsx        # 按餐次分组展示
│   │   ├── restaurant/
│   │   │   ├── DietBadge.tsx        # 饮食标签徽章（含颜色映射）
│   │   │   ├── AllergenWarning.tsx  # 过敏原警告徽章
│   │   │   └── MenuItemCard.tsx     # 菜品卡片
│   │   └── common/
│   │       ├── EmptyState.tsx       # 空状态组件（含插图）
│   │       └── PageSkeleton.tsx     # 页面骨架屏
│   │
│   ├── illustrations/               # 前端插图（由 skill 生成的 SVG 组件）
│   │   ├── HeroIllustration.tsx
│   │   ├── EmptySearchState.tsx
│   │   ├── EmptyLogState.tsx
│   │   └── icons/
│   │       ├── VeganIcon.tsx
│   │       ├── HalalIcon.tsx
│   │       ├── GlutenFreeIcon.tsx
│   │       └── ...（其余饮食类型图标）
│   │
│   ├── store/                       # Zustand 全局状态
│   │   ├── useAuthStore.ts          # 用户登录态
│   │   ├── useSearchStore.ts        # 搜索参数、结果缓存
│   │   └── useDietStore.ts          # 今日饮食日志、档案
│   │
│   ├── api/                         # API 调用函数（封装 Axios）
│   │   ├── client.ts                # Axios 实例（含拦截器）
│   │   ├── auth.ts
│   │   ├── food.ts
│   │   ├── diet.ts
│   │   ├── search.ts
│   │   └── restaurants.ts
│   │
│   ├── hooks/                       # 自定义 Hook
│   │   ├── useSearch.ts             # 搜索逻辑（URL 参数同步）
│   │   ├── useFoodSearch.ts         # 食物搜索（含 LLM fallback 状态）
│   │   └── useDebounce.ts           # 防抖（Autocomplete 用）
│   │
│   ├── constants/
│   │   ├── dietLabels.ts            # 饮食标签配置（label→颜色→图标）
│   │   └── routes.ts                # 路由路径常量
│   │
│   └── types/
│       ├── restaurant.ts            # Restaurant、MenuItem 类型定义
│       ├── food.ts                  # FoodItem、FoodLogEntry 类型
│       └── search.ts                # SearchResult、Facet 类型
│
├── package.json
├── tsconfig.json
└── vite.config.ts                   # Vite 构建配置
```

---

## 2. 路由配置

```tsx
// App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';

const App: React.FC = () => (
  <BrowserRouter>
    <AppLayout>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/search" element={<SearchPage />} />
        <Route path="/restaurant/:id" element={<RestaurantDetailPage />} />
        <Route path="/diet/log" element={<PrivateRoute><DietLogPage /></PrivateRoute>} />
        <Route path="/diet/profile" element={<PrivateRoute><DietProfilePage /></PrivateRoute>} />
        <Route path="/saved" element={<PrivateRoute><SavedPage /></PrivateRoute>} />
        <Route path="/auth/login" element={<LoginPage />} />
        <Route path="/auth/register" element={<RegisterPage />} />
      </Routes>
    </AppLayout>
  </BrowserRouter>
);
```

`PrivateRoute` 组件：未登录时 redirect 到 `/auth/login`，并在 URL 中携带 `redirect` 参数。

---

## 3. 状态管理

### 3.1 useAuthStore（用户认证）

```typescript
// store/useAuthStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  user: User | null;
  isLoggedIn: boolean;
  setUser: (user: User | null) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      isLoggedIn: false,
      setUser: (user) => set({ user, isLoggedIn: !!user }),
      logout: () => set({ user: null, isLoggedIn: false }),
    }),
    { name: 'auth-storage' }
  )
);
```

### 3.2 useSearchStore（搜索状态）

```typescript
// store/useSearchStore.ts
interface SearchState {
  query: string;
  dietLabels: string[];
  priceLevel: number[];
  ratingMin: number | null;
  sortMode: SortMode;
  page: number;
  results: SearchResult | null;
  loading: boolean;
  setParams: (params: Partial<SearchParams>) => void;
  setResults: (results: SearchResult) => void;
}
```

**关键设计：** URL 参数为搜索状态的唯一真实来源（Single Source of Truth）。
`useSearch` Hook 监听 URL params 变化，同步到 store 并触发 API 调用。

---

## 4. 关键组件实现

### 4.1 SearchBar（搜索框 + Autocomplete）

```tsx
// components/search/SearchBar.tsx
const SearchBar: React.FC<SearchBarProps> = ({ onSearch }) => {
  const [inputVal, setInputVal] = useState('');
  const [suggestions, setSuggestions] = useState<Suggestion[]>([]);
  const debouncedVal = useDebounce(inputVal, 200);

  useEffect(() => {
    if (debouncedVal.length < 2) {
      setSuggestions([]);
      return;
    }
    searchApi.getSuggestions(debouncedVal).then(setSuggestions);
  }, [debouncedVal]);

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter') onSearch(inputVal);
    if (e.key === 'Escape') setSuggestions([]);
  };

  return (
    <div className="search-bar-wrapper">
      <AutoComplete
        options={suggestions.map(s => ({ value: s.text, label: s.text }))}
        onSelect={(val) => { setInputVal(val); onSearch(val); }}
      >
        <Input
          value={inputVal}
          onChange={(e) => setInputVal(e.target.value)}
          onKeyDown={handleKeyDown}
          prefix={<SearchOutlined style={{ color: '#AAB4B4' }} />}
          placeholder="搜索素食、清真、无麸质餐厅..."
          style={{ borderRadius: 24, height: 48 }}
        />
      </AutoComplete>
    </div>
  );
};
```

### 4.2 DietBadge（饮食标签徽章）

```tsx
// components/restaurant/DietBadge.tsx
const DIET_BADGE_CONFIG: Record<string, { bg: string; text: string; icon: React.ReactNode }> = {
  vegan:       { bg: '#E8F5E9', text: '#2D9B5A', icon: <VeganIcon width={14} height={14} /> },
  vegetarian:  { bg: '#F1F8E9', text: '#558B2F', icon: <VegetarianIcon width={14} height={14} /> },
  halal:       { bg: '#E3F2FD', text: '#1565C0', icon: <HalalIcon width={14} height={14} /> },
  'gluten-free': { bg: '#FFF8E1', text: '#F57F17', icon: <GlutenFreeIcon width={14} height={14} /> },
  // ...
};

const DietBadge: React.FC<{ label: string; confidence?: 'confirmed' | 'inferred' }> = ({
  label,
  confidence = 'confirmed',
}) => {
  const config = DIET_BADGE_CONFIG[label];
  if (!config) return null;

  return (
    <span
      style={{
        backgroundColor: config.bg,
        color: config.text,
        border: confidence === 'inferred' ? `1px dashed ${config.text}` : 'none',
        borderRadius: 999,
        padding: '2px 10px',
        fontSize: 12,
        display: 'inline-flex',
        alignItems: 'center',
        gap: 4,
      }}
      title={confidence === 'inferred' ? `${label}（推断，非官方确认）` : label}
    >
      {config.icon}
      {label}
    </span>
  );
};
```

### 4.3 FoodSearchModal（含 LLM 确认流程）

```tsx
// components/diet/FoodSearchModal.tsx
const FoodSearchModal: React.FC<Props> = ({ onAdd, onClose }) => {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState<FoodSearchResult | null>(null);
  const [llmConfirming, setLlmConfirming] = useState(false);
  const [selectedFood, setSelectedFood] = useState<FoodItem | null>(null);
  const [quantityG, setQuantityG] = useState<number>(100);

  const handleSearch = async (q: string) => {
    const res = await foodApi.search(q);
    setResults(res);
  };

  const handleLlmConfirm = async (foodData: FoodItem) => {
    setLlmConfirming(true);
    const confirmed = await foodApi.confirmLlm(foodData);
    setSelectedFood({ ...foodData, food_id: confirmed.food_id });
    setLlmConfirming(false);
  };

  // 步骤 1：搜索 → 步骤 2：选择食物 → 步骤 3：输入分量 → 步骤 4：提交
  // ...（多步骤 UI 逻辑）
};
```

### 4.4 NutritionBar（营养进度条）

```tsx
// components/diet/NutritionBar.tsx
const NutritionBar: React.FC<{
  label: string;
  current: number;
  goal: number | null;
  unit: string;
}> = ({ label, current, goal, unit }) => {
  const pct = goal ? Math.min((current / goal) * 100, 100) : null;
  const color = pct === null ? '#2D9B5A'
              : pct >= 100  ? '#E85454'
              : pct >= 80   ? '#FAAD14'
              : '#2D9B5A';

  return (
    <div className="nutrition-bar">
      <div className="nutrition-bar-header">
        <span>{label}</span>
        <span>{current.toFixed(1)}{unit}{goal ? ` / ${goal}${unit}` : ''}</span>
      </div>
      {pct !== null && (
        <Progress
          percent={pct}
          showInfo={false}
          strokeColor={color}
          strokeWidth={8}
          style={{ margin: 0 }}
        />
      )}
    </div>
  );
};
```

---

## 5. API 客户端

```typescript
// api/client.ts
import axios from 'axios';

const client = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000',
  withCredentials: true,   // 必须携带 Cookie
  timeout: 15000,
});

// 响应拦截器：统一处理 401
client.interceptors.response.use(
  (res) => res.data,
  (err) => {
    if (err.response?.status === 401) {
      useAuthStore.getState().logout();
      window.location.href = '/auth/login';
    }
    return Promise.reject(err.response?.data?.error || '请求失败');
  }
);
```

---

## 6. useSearch Hook（URL 参数同步）

```typescript
// hooks/useSearch.ts
import { useSearchParams } from 'react-router-dom';

export function useSearch() {
  const [searchParams, setSearchParams] = useSearchParams();
  const { setResults, setLoading } = useSearchStore();

  const params = useMemo(() => ({
    q: searchParams.get('q') || '',
    dietLabels: searchParams.get('diet_labels')?.split(',').filter(Boolean) || [],
    priceLevel: searchParams.get('price_level')?.split(',').map(Number) || [],
    ratingMin: searchParams.get('rating_min') ? Number(searchParams.get('rating_min')) : null,
    sort: (searchParams.get('sort') || 'default') as SortMode,
    page: Number(searchParams.get('page') || 1),
  }), [searchParams]);

  useEffect(() => {
    if (!params.q && params.dietLabels.length === 0) return;
    setLoading(true);
    searchApi.search(params)
      .then(setResults)
      .finally(() => setLoading(false));
  }, [params]);   // params 变化时自动触发

  const updateParams = (updates: Partial<SearchParams>) => {
    const next = { ...params, ...updates, page: 1 };   // 更新 filter 时重置页码
    setSearchParams(buildSearchParams(next));
  };

  return { params, updateParams };
}
```

---

## 7. 构建与环境变量

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8000

# .env.production
VITE_API_BASE_URL=https://api.dietsearch.example.com
```

```bash
# 开发
npm run dev

# 生产构建
npm run build    # 产物输出到 dist/

# 代码检查
npm run lint
npm run type-check
```

**Vite 配置要点：**
```typescript
// vite.config.ts
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:8000',   // 开发时代理 API，避免 CORS
    },
  },
  build: {
    chunkSizeWarningLimit: 1000,
  },
});
```
